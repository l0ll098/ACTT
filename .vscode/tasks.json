{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm start",
            "type": "shell",
            "command": "npm start",
            "problemMatcher": "$tsc",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}